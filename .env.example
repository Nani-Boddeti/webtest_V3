# Research Document Generator – Environment Variables
#
# Copy this file to .env and replace each placeholder with a real value.
# NEVER commit .env (it is git-ignored).

SEARCH_API_KEY=<to be replaced>
SEARCH_API_URL=<to be replaced>

LLM_API_KEY=<to be replaced>
LLM_API_URL=<to be replaced>
