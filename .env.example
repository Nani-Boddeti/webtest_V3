# Serper.dev key used by the /api/compare route to fetch smartwatch data.
# Get a key from https://serper.dev/ and copy this file to .env.local.
# Never commit a real key to source control.
SERPER_API_KEY=
