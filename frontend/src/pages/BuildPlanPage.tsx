/**
 * Build Plan Kanban board — Part 2 will add:
 * - 4-column board (To Do, In Progress, Done, Blocked)
 * - Draggable TaskCard components
 * - Inline editing, status dropdown, reorder
 */
export default function BuildPlanPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-gray-900">Build Plan</h1>
      <p className="mt-1 text-sm text-gray-500">Kanban board for managing build plan tasks.</p>

      <div className="mt-6 rounded-lg border border-dashed border-gray-300 bg-white p-12 text-center">
        <p className="text-gray-500">Kanban board will be implemented in Part 2.</p>
      </div>
    </div>
  );
}
